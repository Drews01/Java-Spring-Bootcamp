# ===========================================
# Docker Image (set by CI/CD or manually)
# ===========================================
DOCKER_IMAGE=ghcr.io/your-github-username/loan-app:latest

# ===========================================
# Database Configuration (SQL Server)
# ===========================================
DB_NAME=LoanDB
DB_USER=sa
# IMPORTANT: Use a strong password (min 8 chars, uppercase, lowercase, number, special char)
DB_PASSWORD=YourStrong@Password123

# ===========================================
# JWT Security
# ===========================================
# Generate with: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=86400000

# ===========================================
# Mail Configuration (Gmail example)
# ===========================================
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
# Use App Password for Gmail: https://myaccount.google.com/apppasswords
MAIL_PASSWORD=your-app-password

# ===========================================
# Domain (for Nginx SSL)
# ===========================================
DOMAIN=yourdomain.com
